name: Daily Snapshot Scheduler

on:
  schedule:
    # Run daily at 04:00 Berlin time (02:00 UTC in summer, 03:00 UTC in winter)
    - cron: '0 2 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  process-snapshots:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch and Process Snapshots
        run: |
          SUPABASE_URL="${{ secrets.VITE_SUPABASE_URL }}"
          API_KEY="${{ secrets.VITE_SUPABASE_KEY }}"
          
          echo "✅ Using Anon Key (works!)"
          echo "📋 Fetching active schedules from: $SUPABASE_URL"
          
          # Fetch ALL schedules
          RESPONSE=$(curl -s -X GET \
            "${SUPABASE_URL}/rest/v1/scheduled_snapshots?select=id,player_id,scheduled_until,status" \
            -H "apikey: ${API_KEY}" \
            -w "\n%{http_code}")
          
          # Split response and status code
          BODY=$(echo "$RESPONSE" | head -n -1)
          STATUS=$(echo "$RESPONSE" | tail -n 1)
          
          echo "HTTP Status: $STATUS"
          echo "Raw response: $BODY"
          
          if [ "$STATUS" != "200" ]; then
            echo "❌ API Error"
            exit 1
          fi
          
          if ! echo "$BODY" | jq empty 2>/dev/null; then
            echo "❌ Invalid JSON"
            exit 1
          fi
          
          COUNT=$(echo "$BODY" | jq 'length')
          echo "Found $COUNT total schedules"
          
          if [ "$COUNT" -eq 0 ]; then
            echo "✓ No schedules"
            exit 0
          fi
          
          # Filter: active and not expired
          NOW=$(date -u +%s)
          echo "$BODY" | jq -c '.[]' | while read -r schedule; do
            STATUS_VAL=$(echo "$schedule" | jq -r '.status')
            UNTIL=$(echo "$schedule" | jq -r '.scheduled_until')
            UNTIL_EPOCH=$(date -d "$UNTIL" +%s 2>/dev/null || echo 0)
            
            # Skip if expired or not active
            if [ "$STATUS_VAL" != "active" ] || [ "$UNTIL_EPOCH" -le "$NOW" ]; then
              continue
            fi
            
            PLAYER_ID=$(echo "$schedule" | jq -r '.player_id')
            
            echo ""
            echo "🔄 Processing player_id: $PLAYER_ID"
            
            # Fetch player
            PLAYER=$(curl -s -X GET \
              "${SUPABASE_URL}/rest/v1/players?player_id=eq.${PLAYER_ID}" \
              -H "apikey: ${API_KEY}" | jq '.[0]')
            
            PUUID=$(echo "$PLAYER" | jq -r '.puuid // empty')
            GAME_NAME=$(echo "$PLAYER" | jq -r '.gameName // "?"')
            TAGLINE=$(echo "$PLAYER" | jq -r '.tagline // "?"')
            
            if [ -z "$PUUID" ]; then
              echo "  ⚠ No PUUID"
              continue
            fi
            
            echo "  🎮 ${GAME_NAME}#${TAGLINE}"
            echo "    → Saving stats..."
            
            curl -s "https://summonersheet.de/api/db/stats/save?puuid=${PUUID}&player_id=${PLAYER_ID}" > /dev/null 2>&1
            curl -s "https://summonersheet.de/api/db/champion-stats/save?puuid=${PUUID}&player_id=${PLAYER_ID}" > /dev/null 2>&1
            
            echo "  ✅ Done"
          done
          
          echo ""
          echo "✅ Job completed"
